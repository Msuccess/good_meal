import 'package:flutter/material.dart';

class BookMarkScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Container(
          child: Text('Book Mark Works'),
        ),
      ),
    );
  }
}
